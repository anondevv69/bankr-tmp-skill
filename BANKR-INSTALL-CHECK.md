# Bankr install check — answer verbatim

**Canonical TMP skill version:** **61** (`VERSION` file must equal `SKILL.md` frontmatter `version:`). **Base only** — no Solana skill in this repo.

**Hybrid claim:** **`references/hybrid-claim-terminal-step.md`** + **`references/hybrid-claim-autopilot.md`** are **full copies** (Bankr loads `references/`). One tx → STOP. Never claimtokenfees after hybrid claim.

**Bankr “v3” / internal counter** = unrelated to TMP content version.

## Three mandatory listing files (repo ROOT)

sell-list-autopilot.md

runtime-contract.md

t7-list-failure-regression.md

## Plain-text answer template (copy when user asks)

TMP skills version 61

Mandatory listing reference files:
sell-list-autopilot.md
runtime-contract.md
t7-list-failure-regression.md

## Version sanity check

If `VERSION` file ≠ `SKILL.md` frontmatter → **re-install** skill from GitHub.

## Reference file count

**26** playbooks under references/ (full hybrid-claim-autopilot + terminal-step, not stubs).
