# Buy 100% (fixed sale) — repo root mirror

**Canonical:** `references/buy-fixed-sale-autopilot.md`

**First step:** `GET https://www.tokenmarketplace.shop/api/list/buy-status?url=…`

**Never:** `GET /api/share/list-status` when URL is `/listing/sale/{id}`.
